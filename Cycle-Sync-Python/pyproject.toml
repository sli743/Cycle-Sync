[project]
name = "cyclesync"
version = "0.1.0"
description = "Cycle-Sync camera location synchronization experiments"
readme = "README.md"
requires-python = ">=3.10"
license = {file = "LICENSE"}
dependencies = ["numpy>=1.23","scipy>=1.10","pandas>=1.5","matplotlib>=3.7","networkx>=3.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
